#include "main.h"
#include <stdio.h>

/**
 * main - tests function the prints if integer is + or -
 * * Description: positive_or_negative
 * @i: integer
 * Return: 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
