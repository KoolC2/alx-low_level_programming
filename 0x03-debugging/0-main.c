#include "main.h"
#include <stdio.h>

/**
 * positive_or_negative - tests function the prints if integer is positive or negative
 * Description: positive_or_negative
 * Return: 0
 */

int postive_or_negative(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
