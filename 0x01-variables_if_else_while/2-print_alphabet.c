#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *  main - Entry point
 *
 *  Return: Always 0 (Success)
 */
int main(void)
{
	char lwca;

	for (lwca = 'a'; lwca <= 'z'; lwca++);
        {
		putchar(lwca);
	}
	return (0);
}
